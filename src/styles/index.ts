/**
 * Styles - Space Math Commander Design System
 * ===========================================
 *
 * This module re-exports all design tokens for convenient importing.
 *
 * Usage:
 * ```tsx
 * import { colors, typography, borders, glows } from '@/styles';
 * ```
 *
 * Or import from tokens directly:
 * ```tsx
 * import { colors, getTypographyCSS } from '@/styles/tokens';
 * ```
 */

export * from './tokens';
